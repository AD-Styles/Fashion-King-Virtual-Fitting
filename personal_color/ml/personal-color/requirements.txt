# P1 (규칙 두뇌 + selftest): numpy만 필요
numpy>=1.24

# P1.5 얼굴 -> 대표색 추출 (features.py)
# mediapipe(landmark 경로): opencv-contrib·protobuf 동반(별도 opencv 불필요)
mediapipe>=0.10
Pillow>=10.0
# seg 경로(기본/권장): jonathandinu/face-parsing(SegFormer)
transformers>=4.40

# torch/torchvision 은 전용 인덱스에서 요건에 맞게 설치(P1.5 seg + P2 학습 공용):
#   GPU(cu121): pip install --index-url https://download.pytorch.org/whl/cu121 torch torchvision
#   CPU       : pip install torch torchvision

# P2 CNN 학습 추가분 (학습 시 주석 해제)
# scikit-learn>=1.3       # 평가 지표/혼동행렬
# scikit-image>=0.22
# datasets>=2.19          # FairFace(HF) 스트리밍 로드
